<Application x:Class="Cashlane.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Cashlane.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <conv:StatusToColorConverter x:Key="StatusToColorConverter" />
            <conv:StatusToBackgroundConverter x:Key="StatusToBackgroundConverter" />
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <conv:InverseBoolConverter x:Key="InverseBoolConverter" />
            <conv:MoneyFormatConverter x:Key="MoneyFormatConverter" />
            <conv:NullableIntToVisibilityConverter x:Key="NullableIntToVisibilityConverter" />
            <conv:StringToDateTimeConverter x:Key="StringToDateTimeConverter" />
            <conv:EditModeToTitleConverter x:Key="EditModeToTitleConverter" />

            <!-- Colors -->
            <Color x:Key="AccentColor">#0078D4</Color>
            <Color x:Key="AccentHoverColor">#106EBE</Color>
            <Color x:Key="SidebarBgColor">#1B1B1B</Color>
            <Color x:Key="SidebarHoverColor">#2D2D2D</Color>
            <Color x:Key="SidebarActiveColor">#3A3A3A</Color>
            <Color x:Key="BgMicaColor">#F3F3F3</Color>
            <Color x:Key="BorderColor">#E5E5E5</Color>
            <Color x:Key="DangerColor">#C42B1C</Color>
            <Color x:Key="SuccessColor">#0F7B0F</Color>

            <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
            <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}" />
            <SolidColorBrush x:Key="SidebarBgBrush" Color="{StaticResource SidebarBgColor}" />
            <SolidColorBrush x:Key="SidebarHoverBrush" Color="{StaticResource SidebarHoverColor}" />
            <SolidColorBrush x:Key="SidebarActiveBrush" Color="{StaticResource SidebarActiveColor}" />
            <SolidColorBrush x:Key="BgMicaBrush" Color="{StaticResource BgMicaColor}" />
            <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
            <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}" />
            <SolidColorBrush x:Key="TextPrimaryBrush">#1A1A1A</SolidColorBrush>
            <SolidColorBrush x:Key="TextSecondaryBrush">#5C5C5C</SolidColorBrush>
            <SolidColorBrush x:Key="TextTertiaryBrush">#999999</SolidColorBrush>
            <SolidColorBrush x:Key="TextSidebarBrush">#BBBBBB</SolidColorBrush>
            <SolidColorBrush x:Key="TextSidebarActiveBrush">#FFFFFF</SolidColorBrush>
            <SolidColorBrush x:Key="CardBrush">#FFFFFF</SolidColorBrush>
            <SolidColorBrush x:Key="FilterBgBrush">#F7F7F7</SolidColorBrush>
            <SolidColorBrush x:Key="RowAltBrush">#FAFAFA</SolidColorBrush>

            <!-- Font -->
            <FontFamily x:Key="AppFont">Segoe UI, Microsoft YaHei, sans-serif</FontFamily>

            <!-- Base Styles -->
            <Style x:Key="BaseButton" TargetType="Button">
                <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Height" Value="32" />
                <Setter Property="Padding" Value="14,0" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="#D1D1D1" />
                <Setter Property="Background" Value="{StaticResource CardBrush}" />
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#F7F7F7" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="#BBBBBB" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
                <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
                <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
            </Style>

            <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
                <Setter Property="Height" Value="28" />
                <Setter Property="Padding" Value="10,0" />
                <Setter Property="FontSize" Value="12" />
            </Style>

            <Style x:Key="SmallPrimaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                <Setter Property="Height" Value="28" />
                <Setter Property="Padding" Value="10,0" />
                <Setter Property="FontSize" Value="12" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
