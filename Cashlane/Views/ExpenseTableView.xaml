<UserControl x:Class="Cashlane.Views.ExpenseTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Cashlane.Converters">

    <Border Background="White" BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1" CornerRadius="12">
        <DockPanel>
            <!-- Toolbar -->
            <Border DockPanel.Dock="Top"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="20,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="➕ 新增"
                                Style="{StaticResource SmallPrimaryButton}"
                                Command="{Binding AddCommand}" />
                        <Button Content="✏️ 编辑"
                                Style="{StaticResource SmallButton}"
                                Command="{Binding EditCommand}"
                                Margin="6,0,0,0" />
                        <Button Content="🗑️ 删除"
                                Style="{StaticResource SmallButton}"
                                Command="{Binding DeleteCommand}"
                                Margin="6,0,0,0" />
                        <Border Width="1" Height="20" Background="{StaticResource BorderBrush}"
                                Margin="8,0" />
                        <Button Content="📥 导入"
                                Style="{StaticResource SmallButton}"
                                Command="{Binding ImportCommand}" />
                        <Button Content="📤 导出"
                                Style="{StaticResource SmallButton}"
                                Command="{Binding ExportCommand}"
                                Margin="6,0,0,0" />
                    </StackPanel>

                    <TextBlock Grid.Column="1" VerticalAlignment="Center"
                               FontSize="12" Foreground="{StaticResource TextTertiaryBrush}">
                        <Run Text="共 " />
                        <Run Text="{Binding TotalCount, Mode=OneWay}" />
                        <Run Text=" 条" />
                    </TextBlock>
                </Grid>
            </Border>

            <!-- Status Bar (Bottom) -->
            <Border DockPanel.Dock="Bottom"
                    Background="{StaticResource FilterBgBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,1,0,0"
                    Padding="20,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontSize="12" Foreground="{StaticResource TextSecondaryBrush}">
                            <Run Text="记录数: " />
                            <Run Text="{Binding TotalCount, Mode=OneWay}" FontWeight="SemiBold" />
                            <Run Text="　|　合计金额: " />
                        </TextBlock>
                        <TextBlock FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource DangerBrush}">
                            <TextBlock.Text>
                                <Binding Path="TotalAmount" StringFormat="¥{0:N2}" />
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="‹" Style="{StaticResource SmallButton}" Width="28" />
                        <Button Content="1" Style="{StaticResource SmallPrimaryButton}" Width="28" Margin="4,0,0,0" />
                        <Button Content="2" Style="{StaticResource SmallButton}" Width="28" Margin="4,0,0,0" />
                        <Button Content="3" Style="{StaticResource SmallButton}" Width="28" Margin="4,0,0,0" />
                        <Button Content="…" Style="{StaticResource SmallButton}" Width="28" Margin="4,0,0,0" />
                        <Button Content="›" Style="{StaticResource SmallButton}" Width="28" Margin="4,0,0,0" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Data Grid -->
            <DataGrid ItemsSource="{Binding Expenses}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      CanUserSortColumns="True"
                      CanUserResizeColumns="True"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                      BorderThickness="0"
                      FontSize="13"
                      FontFamily="{StaticResource AppFont}"
                      RowHeight="40"
                      HeadersVisibility="Column"
                      SelectedItem="{Binding SelectedExpense}"
                      MouseDoubleClick="OnRowDoubleClick">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource FilterBgBrush}" />
                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="Padding" Value="14,10" />
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#CCE4F7" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="rgba(0,120,212,0.06)" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="14,11" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="单据ID" Binding="{Binding DocId}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="填单日期" Binding="{Binding EntryDate}" Width="100" />
                    <DataGridTextColumn Header="发生日期" Binding="{Binding OccurDate}" Width="100" />
                    <DataGridTextColumn Header="公司" Binding="{Binding CompanyName}" Width="90" />
                    <DataGridTextColumn Header="部门" Binding="{Binding DeptName}" Width="80" />
                    <DataGridTextColumn Header="一级科目" Binding="{Binding Cat1Name}" Width="90" />
                    <DataGridTextColumn Header="二级科目" Binding="{Binding Cat2Name}" Width="80" />
                    <DataGridTextColumn Header="发票内容" Binding="{Binding InvoiceContent}" Width="150" />
                    <DataGridTextColumn Header="费用明细" Binding="{Binding Detail}" Width="*" MinWidth="120" />

                    <DataGridTextColumn Header="数量" Binding="{Binding Quantity}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="单价" Width="90">
                        <DataGridTextColumn.Binding>
                            <Binding Path="UnitPrice" Converter="{StaticResource MoneyFormatConverter}" />
                        </DataGridTextColumn.Binding>
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="预付" Width="90">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Prepaid" Converter="{StaticResource MoneyFormatConverter}" />
                        </DataGridTextColumn.Binding>
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="报销费用" Width="100">
                        <DataGridTextColumn.Binding>
                            <Binding Path="ReimburseAmount" Converter="{StaticResource MoneyFormatConverter}" />
                        </DataGridTextColumn.Binding>
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="供应商" Binding="{Binding ContactName}" Width="90" />
                    <DataGridTextColumn Header="报销人" Binding="{Binding Reimbursee}" Width="80" />

                    <DataGridTemplateColumn Header="状态" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="10,2"
                                        Background="{Binding ReimburseStatus, Converter={StaticResource StatusToBackgroundConverter}}">
                                    <TextBlock Text="{Binding ReimburseStatus}"
                                               FontSize="12" FontWeight="Medium"
                                               Foreground="{Binding ReimburseStatus, Converter={StaticResource StatusToColorConverter}}"
                                               HorizontalAlignment="Center" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="备注" Binding="{Binding Notes}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Border>
</UserControl>
